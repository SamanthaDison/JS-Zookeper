export class Mink {
    constructor(name, color, gender, texture, cuteFactor) {
        this.name = name;
        this.color = color;
        this.gender = gender;
        this.texture = texture;
        this.cuteFactor = cuteFactor;
    }

}
let manny = new Mink("Manny", "Grey", "M", "Smooth", "Extremely")